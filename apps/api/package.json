{
  "name": "@damay/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'api dev — implemented in Phase 2 (Python/FastAPI)'",
    "build": "echo 'api build — Python; no JS build step'",
    "lint": "echo 'noop'",
    "typecheck": "echo 'noop'",
    "test": "echo 'noop'"
  }
}
